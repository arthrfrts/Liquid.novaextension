<?xml version="1.0" encoding="UTF-8"?>
<syntax name="liquid-md">
	<meta>
		<name>Markdown (Liquid)</name>
		<type>markup</type>
		<preferred-file-extension>md</preferred-file-extension>
		<parent>liquid</parent>
	</meta>

	<detectors>
		<combo priority="0.9">
			<extension>md</extension>
			<match-content lines="100">\{\%</match-content>
			<match-content lines="100">\{\{</match-content>
		</combo>
	</detectors>

	<indentation>
		<increase>
			<expression>(?x)
				(&lt;(?!\?|(?:area|base|br|col|embed|hr|img|input|link|meta|param|source|track|wbr)\b)
				([-_\.A-Za-z0-9]+)(?=\s|&gt;)\b[^&gt;]*&gt;[^&lt;]*$)
				|(&lt;!--\s*$)
			</expression>
		</increase>
		<decrease>
			<expression>(?x)
				^\s*
				(&lt;/[-_\.A-Za-z0-9]+\b[^>]*&gt;
				|--&gt;
			)</expression>
		</decrease>
	</indentation>

	<comments>
		<multiline>
			<starts-with>
				<expression>{% comment %}</expression>
			</starts-with>
			<ends-with>
				<expression>{% endcomment %}</expression>
			</ends-with>
		</multiline>
	</comments>

	<brackets>
		<pair open="[" close="]" />
		<pair open="(" close=")" />
	</brackets>

	<surrounding-pairs>
		<pair open="&lt;" close="&gt;" />
		<pair open="[" close="]" />
		<pair open="(" close=")" />
		<pair open="&apos;" close="&apos;" />
		<pair open="&quot;" close="&quot;" />
		<pair open="`" close="`" />
	</surrounding-pairs>
	
	<tree-sitter language="liquid">
		<highlights />
		<injections />
	</tree-sitter>
</syntax>
